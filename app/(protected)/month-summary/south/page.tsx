import MonthSummaryView from "../MonthSummaryView";

/** ສະຫຼຸບເດືອນ · ພາກໃຕ້ — ແຂວງສະຫວັນນະເຂດລົງໄປ. */
export default function MonthSummarySouthPage() {
  return <MonthSummaryView region="south" />;
}
